import { backendApiGatewayUtils } from './utils';

describe('backendApiGatewayUtils', () => {
  it('should work', () => {
    expect(backendApiGatewayUtils()).toEqual('backend-api-gateway-utils');
  });
});
